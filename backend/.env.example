# Backend Environment Variables
# These should be set in your cloud platform (Railway, Render, AWS, etc.)

# Spring Profile
SPRING_PROFILES_ACTIVE=prod

# Database Configuration
DATABASE_URL=jdbc:postgresql://your-db-host:5432/nxtclass
DB_USERNAME=your-db-username
DB_PASSWORD=your-db-password

# JWT Configuration
# Generate a secure secret using: openssl rand -base64 64
JWT_SECRET=your-super-secret-jwt-key-at-least-256-bits-long

# CORS Configuration
FRONTEND_URL=https://your-frontend-url.com

# Optional: Server Port (defaults to 8080)
SERVER_PORT=8080

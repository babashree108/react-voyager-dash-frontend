# ================================================
# PRODUCTION ENVIRONMENT
# Ports: Frontend 5000, Backend 8082, DB 5434
# ================================================

# Database Configuration
DB_NAME=nxtclass_production
DB_USER=nxtclass_prod_user
DB_PASSWORD=CHANGE_THIS_TO_VERY_SECURE_PASSWORD

# JWT Configuration (generate with: openssl rand -base64 64)
JWT_SECRET=CHANGE_THIS_TO_VERY_SECURE_JWT_SECRET_MIN_256_BITS

# Frontend API URL
VITE_API_URL=http://localhost:8082/api

# Build metadata
BUILD_DATE=2025-12-07
VERSION=v1.0.0
